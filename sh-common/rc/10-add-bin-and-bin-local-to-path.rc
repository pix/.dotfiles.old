#!/bin/sh

addpath() {
    for dir in $(find "$1"/ -follow -maxdepth 2 -type d ! -name .git); do
        export PATH=$PATH:$dir
    done
}
[ -d ~/.bin/ ] && addpath ~/.bin || true
[ -d ~/.bin-local/ ] && addpath ~/.bin-local || true

